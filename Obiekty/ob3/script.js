var defaultCourse = {
    students: ['Marek', 'Arek', 'Darek', 'Czarek'],
    place: 'IAS',
    time: '5h',
    breakTime: '1h'
}


function fillCourse(course) {
    course.time = defaultCourse;
    students.name = defaultName;
    place.name = defaultName;
    breakTime = defaultTime;
}


Object.keys(defaultCourse);
Object.values(defaultCourse);
Object.entries(defaultCourse);

console.log(defaultCourse);

